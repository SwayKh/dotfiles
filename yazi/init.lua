require("full-border"):setup({
	type = ui.Border.ROUNDED,
})

require("personal"):setup()

-- To Implement from lf
-- Pick Random function
-- Move to new directory
-- Drag n Drop function
-- Mappings
-- Settings
-- folder rules
-- Fix the statusline colors
-- Make all changed keymaps into prepend_keymap, keep the default config as is,  Easier to share
-- Fix the hovered file issue
-- Make the share command work with 0x0.st
-- Fix the Tabs no folder rules issue
-- Archive functionality
